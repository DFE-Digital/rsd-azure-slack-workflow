locals {
  environment         = var.environment
  project_name        = var.project_name
  azure_location      = var.azure_location
  resource_group_bins = var.resource_group_bins

  tags = var.tags
}
